# Copy to .env.local and fill in. .env.local is gitignored.
# Klipy GIF API key (https://klipy.com — replaces Tenor, shut down 2026-06-30).
# In CI the release workflow injects it from the KLIPY_API_KEY repo secret.
VITE_KLIPY_API_KEY=
